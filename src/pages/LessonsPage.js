import * as React from 'react';
import {Typography} from "@mui/material";

class LessonsPage extends React.Component {
    render() {
        return (
            <Typography variant="h1">Lessons page</Typography>
        );
    }
}

export default LessonsPage;
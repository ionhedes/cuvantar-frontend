import * as React from 'react';
import {Typography} from "@mui/material";

class FlashcardPage extends React.Component {
    render() {
        return (
            <Typography variant="h1">All flashcards page</Typography>
        );
    }
}

export default FlashcardPage;